const {
  BinarySearchTree,
} = require('@datastructures-js/binary-search-tree')

const print = console.log

print("tree = \"Ash\"")
tree = "Ash"
print("typeof tree: " + typeof tree)
print("tree.constructor.name: " + tree.constructor.name)
print("\n\n\n")
print("tree = new BinarySearchTree()")
tree = new BinarySearchTree()
print("typeof tree: " + typeof tree)
print("tree.constructor.name: " + tree.constructor.name)